// constants.js

import * as THREE from 'three';

export const defaultColor = new THREE.Color(0x00ffff);  // Cyan for any other realm
export const notListedColor = new THREE.Color(0x808080);  // Gray for blocks not listed in the CSV
export const longTouchDuration = 500;  // 500 ms for long touch
